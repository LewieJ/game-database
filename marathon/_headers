# When this folder is the static asset root (e.g. marathon-only Worker deploy), these apply.
# For https://gdb.gg (repo root deploy), use the repo root _headers so /sitemap.xml is not covered by a blanket rule.

# IndexNow key — plain text
/d86078e67f7c48eaaccd4c54ece9ab6b.txt
  Content-Type: text/plain

/*
  X-Robots-Tag: index, follow, max-snippet:-1, max-image-preview:large, max-video-preview:-1
  Content-Security-Policy-Report-Only: frame-ancestors 'self'
